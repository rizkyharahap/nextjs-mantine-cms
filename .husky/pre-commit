echo '🏗️👷 Styling your project before committing👷‍♂️🏗️'
echo 'please be patient, this may take a while...'

# Check ESLint Standards
bun run lint || npm run lint ||
(
    echo '🔨❌ Yoo, you have a problem in your code. Check linter 🔨❌
          Run npm run lint:fix, add changes and try commit again.';
    false;
)

# Check Prettier Standards
bun run prettier || npm run prettier ||
(
    echo '🔨❌ Yoo, you have a problem in your code. Check prettier 🔨❌
          Run npm run prettier, add changes and try commit again.';
    false;
)

echo '🎉 No error found: committing this now.... ✨🚀🏄‍♂️🍻'
